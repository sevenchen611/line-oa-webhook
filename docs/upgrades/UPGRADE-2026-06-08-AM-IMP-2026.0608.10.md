# Upgrade Version Record

Version: `AM-IMP-2026.0608.10`

Improvement name: Notion database view layout registry

Target project: 7AM

Upgrade date: 2026-06-08

Owner: Seven / Codex

## Purpose

Define and apply the first durable Notion database table layout for AM-style projects.

This version applies the shared LINE group options default table layout to 7AM.

## Scope

| Item | Included? | Notes |
| --- | --- | --- |
| Notion view update | Yes | Updated the 7AM LINE group options Default view. |
| Notion schema update | No | 7AM already had `候選來源權責項目`. |
| AMCore definition | Yes | Layout stored in AMCore config and upgrade package. |
| Secrets or live data | No | No tokens, customer records, or project data were stored in AMCore. |
| Render deploy | No | This is a Notion view/layout governance change only. |

## Applied Layout

Visible columns in order:

1. `總控專案`
2. `群組顯示名稱`
3. `LINE對話名稱`
4. `候選來源權責項目`

## Data Isolation Check

| Check | Result |
| --- | --- |
| Uses only 7AM Notion objects | Passed. Existing 7AM relation property was used. |
| Copies HOZO AM data | Passed. No HOZO row data or IDs were copied into 7AM records. |
| Stores secrets in AMCore | Passed. AMCore stores only logical layout definitions and property names. |

## Verification

| Step | Actual Result |
| --- | --- |
| Notion schema | `候選來源權責項目` already existed on the 7AM LINE group options database. |
| Notion view | Default view display properties are `總控專案`, `群組顯示名稱`, `LINE對話名稱`, `候選來源權責項目`. |
| AMCore package check | `AM-IMP-2026.0608.10` package completeness check passed. |

## Rollback Plan

Restore the previous 7AM Notion Default view display settings if needed.

Keep the AMCore layout registry record unless the layout is intentionally deprecated.

## Final Status

Installed
