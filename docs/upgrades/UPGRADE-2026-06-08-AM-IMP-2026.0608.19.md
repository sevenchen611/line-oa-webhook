# UPGRADE 2026-06-08 AM-IMP-2026.0608.19

## Summary

Installed the AMCore total-control task table source text hide rule.

## Local Changes

- Updated 7AM `總控任務庫` `Default view` so `來源原文` is no longer displayed in the table header.
- Updated 7AM `總控任務庫` `待確認 View` so `來源原文` is no longer displayed in the table header.
- Kept the `來源原文` property in the database schema.
- Did not erase existing source text values.

## Verification

- Notion view update returned updated display property lists without `來源原文`.
- The 7AM task data source still includes the `來源原文` property.

## Rollback

Add `來源原文` back to the affected Notion table views if the controller wants the source text visible in table headers again.
