# Upgrade Version Record

Version: `AM-IMP-2026.0608.09`

Improvement name: Immediate LINE command conversation mode

Target project: 7AM

Upgrade date: 2026-06-08

Owner: Seven / Codex

## Purpose

Turn `Seven Junior` / `7Junior` prefixed LINE messages into immediate assistant commands instead of silent queue-only requests.

This version specifically fixes the gap found during live testing:

1. User asks: `Seven Junior，目前有哪些待辦？`
2. Seven Jr. replies with a numbered task list.
3. User asks: `Seven Junior，請你把第二個任務打開給我看，我想做一些更新。`
4. Seven Jr. should understand that "the second task" refers to the previous task list and reply with task details.

## Scope

| Item | Included? | Notes |
| --- | --- | --- |
| Code changes | Yes | `src/server.js` now has immediate command parsing, recent task-list memory, and open-task-detail replies. |
| Documentation changes | Yes | README, manifest, shared registry, and this upgrade record updated. |
| Environment variables | No new vars | Uses existing SevenAM LINE, Notion, task database, and Codex command queue settings. |
| Notion schema changes | No | Reads existing `總控任務庫` and writes existing LINE/Codex command records. |
| Render sync/deploy | Yes | GitHub push completed and production health verified on Render. |

## Files Changed

| File | Change |
| --- | --- |
| `src/server.js` | Added `parseImmediateCommand`, `buildOpenTaskDetailReply`, recent per-conversation task list memory, task detail lookup, and acknowledgement fallback. |
| `README.md` | Added LINE immediate command behavior and examples. |
| `docs/project-improvement-manifest.md` | Marks this improvement as installed in 7AM. |
| `docs/upgrades/UPGRADE-2026-06-08-AM-IMP-2026.0608.09.md` | This version record. |
| `D:\Codex_project\AM_SHARED_VERSION_UPGRADES\SHARED_IMPROVEMENT_REGISTRY.md` | Adds shared reusable version row. |

## Environment Variables

| Env var | Required? | Project-specific? | Notes |
| --- | --- | --- | --- |
| `LINE_CHANNEL_ACCESS_TOKEN` | Yes | Yes | SevenAM LINE OA token. |
| `LINE_CHANNEL_SECRET` | Yes | Yes | SevenAM LINE webhook signature verification. |
| `NOTION_TOKEN` | Yes | Yes | SevenAM Notion integration token. |
| `SEVEN_TASKS_DATA_SOURCE_ID` | Yes | Yes | SevenAM total control task database. |
| `SEVEN_CODEX_COMMANDS_DATA_SOURCE_ID` | Recommended | Yes | Stores unsupported immediate commands for Codex follow-up. |

## Data Isolation Check

| Check | Result |
| --- | --- |
| Uses SevenAM LINE only | Pass. Uses project-local `LINE_*` env vars. |
| Uses SevenAM Notion only | Pass. Uses `SEVEN_TASKS_DATA_SOURCE_ID` and existing SevenAM data source guard. |
| Copies HOZO data | Pass. No HOZO token, data source ID, Render ID, or task data copied. |
| Sensitive commands | Pass. High-risk command text is acknowledged but not directly executed. |

## Verification

| Step | Expected Result | Actual Result |
| --- | --- | --- |
| Syntax check | `node --check src/server.js` passes. | Passed locally. |
| Local health flag | `/health` exposes `immediateCommandEnabled=true`. | Passed on local port 3031. |
| Production health flag | `/health` exposes `immediateCommandEnabled=true`. | Passed on Render; prefixes include `Seven Junior`, `7Junior`, and `7 Junior`. |
| Live task list | `Seven Junior，目前有哪些待辦？` returns numbered tasks. | Previously verified by user before this version. |
| Live task detail | `Seven Junior，打開第 2 個任務` returns task detail and Notion URL. | Pending production deployment and user test. |

## Rollback Plan

Remove immediate command parsing and recent task-list memory from `src/server.js`. The original task-list query command can remain active independently.

## Final Status

Deployed
