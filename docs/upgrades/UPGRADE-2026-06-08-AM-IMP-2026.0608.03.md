# Upgrade Version Record

Version: `AM-IMP-2026.0608.03`

Improvement name: LINE task-query reply command

Target project: 7AM

Upgrade date: 2026-06-08

Owner: Seven / Codex

## Purpose

Let a SevenAM user ask Seven Jr. in LINE for current open tasks, so the assistant can answer from the SevenAM total control task database instead of only sending scheduled reports.

Example user intent:

```text
Seven Junior 目前有哪些待辦？
```

## Scope

| Item | Included? | Notes |
| --- | --- | --- |
| Code changes | Yes | Added LINE text command detection and task-list reply builder. |
| Documentation changes | Yes | README, manifest, and this upgrade record updated. |
| Environment variables | Existing | Uses `SEVEN_TASKS_DATA_SOURCE_ID`. |
| Notion schema changes | No | Uses existing SevenAM `總控任務庫`. |
| Render sync/deploy | Config already has task database ID | Production deploy still needs Render verification after GitHub push. |

## Files Changed

| File | Change |
| --- | --- |
| `src/server.js` | Added task-query command detection, open task query, simple actor filtering, and LINE reply formatting. |
| `README.md` | Documented `SEVEN_TASKS_DATA_SOURCE_ID` as the task-query source. |

## Environment Variables

| Env var | Required? | Project-specific? | Notes |
| --- | --- | --- | --- |
| `SEVEN_TASKS_DATA_SOURCE_ID` | Yes | Yes | SevenAM total control task database. |
| `NOTION_TOKEN` | Yes | Yes | SevenAM Notion token. |
| `LINE_CHANNEL_ACCESS_TOKEN` | Yes | Yes | SevenAM LINE OA token for replying. |

## Data Isolation Check

| Check | Result |
| --- | --- |
| Uses SevenAM task database | Pass. Reads `SEVEN_TASKS_DATA_SOURCE_ID`. |
| Copies HOZO task database ID | Pass. No HOZO IDs copied. |
| Stores raw LINE message first | Pass. Command reply is built after the incoming event is stored when Notion is configured. |

## Verification

| Step | Expected Result | Actual Result |
| --- | --- | --- |
| Syntax check | `node --check src/server.js` passes. | Passed locally. |
| Local health check | Webhook health shows task query support. | Passed; `taskQueryReplyEnabled=true`. |
| Safe LINE command test | Avoid creating fake raw LINE events or using invalid reply token. | Not run intentionally; production LINE test is pending after deploy. |

## Rollback Plan

Remove `isTaskListCommandText`, `buildTaskListReply`, and related task query helpers from `src/server.js`. Existing report commands remain unchanged.

## Final Status

Installed
