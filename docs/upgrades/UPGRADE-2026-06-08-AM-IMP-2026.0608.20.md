# UPGRADE 2026-06-08 AM-IMP-2026.0608.20

## Summary

Added a SevenAM-only `今天的行程安排` section to the 08:30 report.

## Local Changes

- Updated `scripts/generate-0800-daily-report-preview.js`.
- Added a Calendar count card to the report metrics.
- Added a Calendar summary card to `今日開局摘要`.
- Added a full `今天的行程安排` section before `專案狀態掃描`.
- Added Calendar input environment examples to `.env.example`.

## Calendar Input

The report can read Calendar events from either:

```text
SEVEN_GOOGLE_CALENDAR_EVENTS_JSON
SEVEN_GOOGLE_CALENDAR_EVENTS_FILE
```

When no Calendar input is available, the report displays an explicit not-connected state instead of showing fake schedule data.

## Verification

- `node --check scripts\generate-0800-daily-report-preview.js` passed.
- Preview generation passed with no Calendar input.
- Preview generation passed with temporary simulated Calendar event JSON.

## HOZO AM

This version is not installed in HOZO AM by owner request.
